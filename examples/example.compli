def main(): int = foo(3) + bar(true, 3)

def foo(x: int): int = (
    let double = x + x in (
        if double < 10
        then double
        else x
    )
)

def bar(cond: bool, n: int): int = (
    if cond then n else 0
)
